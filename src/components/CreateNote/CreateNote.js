const CreateNote = () => {
  return (
    <div>
      <form>
        <h2>Write a Note:</h2>
        <textarea></textarea>
        <button>Submit</button>
        <button>Clear</button>
      </form>
    </div>
  );
};

export default CreateNote;
